export type TlowerSBdata = {
  btnId: number;
  btnName: string;
  btnFxn: () => void;
  btnIcon: React.ReactElement;
};

export type Tnavdata = {
  linkId: number;
  linkname: string;
  linkref: string;
  icon: React.ReactElement;
};
