import SearchBar from "./SearchBar";

const LDAll = () => {
  return (
    <div className="flex w-1/2 flex-col justify-center top-0 relative">
      <SearchBar />
    </div>
  );
};

export default LDAll;
