"use server";

import { TcreateLoanParams } from "@/types";
import { createClient } from "../supabase/server";

export const fetchHomeFeed = async (params: TcreateLoanParams) => {
  const supabase = createClient();

  try {
  } catch (error) {}
};
